@inject IToaster _toaster
@inject IStringLocalizer<Resource> _localizer

@code {
	public void Toast(HttpResponseMessage msg)
	{
		if (msg.IsSuccessStatusCode)
			_toaster.Success(_localizer["Hoàn thành"]);
		else
			_toaster.Error(_localizer["Đã xảy ra lỗi"]);
	}
}
