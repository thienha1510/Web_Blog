@page
@model Blogifier.Pages.ErrorModel

<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Error</title>
    <link href="~/css/bootstrap/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/app.css" rel="stylesheet" />
</head>

<body>
    <div class="main">
        <div class="content px-4">
            <h1 class="text-danger">Đã xảy ra lỗi</h1>
            <h2 class="text-danger">
                Đã xảy ra lỗi khi xử lý yêu cầu của bạn.
            </h2>

            @if (Model.ShowRequestId)
            {
                <p>
                    <strong>Yêu cầu ID:</strong> <code>@Model.RequestId</code>
                </p>
            }

            <h3>Hiện đang giai đoạn phát triểu</h3>
            <p>
                <strong>
                    Chuyển sang môi trường phát triển hiển thị thông tin chi tiết về lỗi đã xảy ra.
                </strong>
            </p>
            <p>
                Môi trường Phát triển sẽ không được kích hoạt cho các ứng dụng đang triển khai. Nó có thể dẫn đến việc hiển thị thông tin nhạy cảm từ các trường hợp ngoại lệ cho người dùng cuối.
            </p>
        </div>
    </div>
</body>

</html>
